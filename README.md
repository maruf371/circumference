# circumference
If you give it the radius of any circle it will give you the circumference
import math
radius = float(input("give the number: "))
circumference = 2 * math.pi * radius
print = circumference
